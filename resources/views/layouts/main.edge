<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>
  <body class="container mt-2">
    <h1 class="display-4">{{title}}</h1>
    <hr class="my-2">
    <div class="col-3 d-flex align-items-end">
      @if (session.get('code'))
      <a href="{{ route('auth.logout') }}" class="badge bg-secondary">ログアウト</a>
      @else
      <a href="{{ route('auth.login.show') }}" class="badge bg-primary">ログイン</a>
      @end
    </div>
    @!section('body')

    <footer>
      <div class="m-4">&nbsp;</div>
      <div class="fixed-bottom bg-primary p-2" style="width: 100%">
        <h6 class="text-center text-white m-0">&copy; 2023 Shunta, Takazato.</h6>
      </div>
    </footer>
  </body>
</html>
